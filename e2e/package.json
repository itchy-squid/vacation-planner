{
  "name": "vacation-planner-e2e",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "End-to-end tests: drive the deployed dev site as a dedicated test account. See README.md.",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "report": "playwright show-report",
    "login": "node scripts/login.mjs"
  },
  "devDependencies": {
    "@playwright/test": "^1.56.0"
  }
}
